/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Add allowed external image domains here
    },
  };
  
  export default nextConfig;